<UserControl
    x:Class="RainDayForUAP.RainyDaySystem.RainyDayCanvas"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" SizeChanged="UserControl_SizeChanged"
    d:DesignWidth="400" Unloaded="UserControl_Unloaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            
        </Grid>
        <Grid Grid.Row="1">
            <Rectangle Name="bg" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >

            </Rectangle>
            <canvas:CanvasAnimatedControl  x:Name="canvas" Width="{Binding ElementName=bg,Path=ActualWidth}" Height="{Binding ElementName=bg,Path=ActualHeight}"
                                      Update="Canvas_Update"
                                      Draw="Canvas_Draw"
                                      CreateResources="Canvas_CreateResources">

            </canvas:CanvasAnimatedControl>
        </Grid>
       
     
        <!--<Image Name="imgbg" Stretch="Uniform" Source="1.jpg"/>-->
    </Grid>
</UserControl>
